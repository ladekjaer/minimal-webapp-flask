# Minimal Webapp with Flask

This is a minimal webapp written for educational purposes.

With Docker installed and running, try this app out by running

```bash
docker compose up -d
```

For an example using Node.js, see the repository [minimal-webapp](https://github.com/ladekjaer/minimal-webapp).
